export const HandleSelectedFiles = event => {
    const uploadedFiles = event.target.files;
    console.log(uploadedFiles)
  };

export const one = (fileInput) => 
{
   
}

export const two = (fileInput) => 
{
    console.log("boob")
    
}
    
export const three = (fileInput) => 
{
    console.log("dood")
}